# Design Decisions

重要な設計判断と、その時点の理由を記録します。DB設計、認証、個人情報、配置、外部サービス依存を変更する場合はこのファイルを更新します。

## 2026-09-01: MVPはRails一体型アプリで進める

- 状態: 採用
- 関連: `SPEC.md` 10章
- 判断: MVPではRailsのサーバーサイドレンダリング、PostgreSQL、Hotwire/Turbo/Stimulus、Tailwind CSSを基本構成にする。
- 理由: 1か月MVPでは、認証、DB、画面、管理機能、テストを1リポジトリで扱える構成の方が実装と運用確認を小さく保てるため。
- 影響: SPAと公開JSON APIの分離はMVP対象外。必要になった場合は仕様変更として扱う。

## 2026-09-01: 生成AI会話と発音採点はMVP対象外にする

- 状態: 採用
- 関連: `SPEC.md` 3.3、19章
- 判断: 実行時の生成AI会話、音声認識、発音自動採点はP2に置く。
- 理由: 安全性、評価品質、実機検証、外部サービス管理の負荷が高く、P0の学習・復習・管理機能を圧迫するため。
- 影響: 教材は事前作成した本文、翻訳、音声を使う。

## 2026-09-01: 教材整合性はDB制約・モデル検証・YAML検証で守る

- 状態: 採用
- 関連: `SPEC.md` 6章、8章、21章 `GAP-01`
- 判断: Course、Lesson、Phrase、Dialogue、DialogueLine、Quiz、QuizOptionをActive Recordモデルとして追加し、教材IDの一意性、Quizの同一Lesson参照、`option_locale`の許可値、教材YAMLの重複・参照・標準件数検証を複数層で確認する。
- 理由: 画面モックの固定配列では、別レッスン混入、意図しない問題重複、公開条件不足を自動検出できないため。
- 影響: 既存履歴テーブルはまだ存在しないため破壊的影響はない。承認済み教材が不足しているため、追加した教材YAMLは下書き扱いとし、標準件数不足は公開前に解消する。

## 2026-09-08: 利用者認証はGoogle OAuthとRailsセッションで行う

- 状態: 採用
- 関連: `SPEC.md` FR-L01、FR-L14、15.1
- 判断: 独自の利用者DBとパスワード認証を持たず、Google OAuthでログインする。ログイン後のGoogleアカウント情報はRailsセッションに保持する。
- 理由: 公開プロトタイプでは利用者DBとパスワード管理を小さくし、Googleアカウント側の認証に委ねる方が運用と個人情報保持を抑えられるため。
- 影響: 管理者権限は `GOOGLE_ADMIN_EMAILS`、停止アカウントは `GOOGLE_STOPPED_EMAILS` の環境変数で制御する。Can do自己評価は永続DBではなくログインセッション内に保存する。
- 未決: 企業利用時にGoogle Workspaceドメイン制限、管理者画面での停止設定、学習履歴の永続保存を行うかは別途判断する。

# AGENTS.md

## プロジェクト概要

このリポジトリでは、日本で働く外国人が職場で使う実践的な日本語を学ぶための、1か月開発MVP Webアプリ **「しごと日本語 / Work Nihongo」** を実装します。

仕様の正本は `SPEC.md` です。実装タスクと進捗管理は `TASKS.md` に従ってください。

## 変更前に必ず読むもの

1. `SPEC.md` を読み、特に今回の作業に関連する機能要件IDを確認してください。
2. `TASKS.md` の現在の対象タスクを確認してください。
3. アーキテクチャ、認証、データ保存期間、デプロイ、外部サービスを変更する場合は、`docs/decisions.md` を確認してください。
4. 編集を始める前に、既存のテストと関連箇所のコードを確認してください。

## MVPの範囲

ユーザーが `SPEC.md` を明示的に変更しない限り、次の機能は実装しないでください。

- 実行時に生成AIと自由会話する機能
- 音声認識または発音採点
- 動画教材、チャット、SNS機能、決済、広告
- 一般公開の利用者登録、ソーシャルログイン、SSO、ネイティブスマートフォンアプリ
- 完全なオフラインモード
- SPAとAPIを分離した構成

依頼によって範囲が拡大する場合は、影響を説明し、現在の変更とは分けて扱ってください。

## 推奨技術構成

- プロジェクトで採用している Ruby on Rails 8.x の安定版
- PostgreSQL
- Hotwire／Turbo／Stimulusを使用したサーバーサイドレンダリングHTML
- Tailwind CSS
- Rails I18n
- `has_secure_password` とRailsセッション
- MinitestとRails System Test
- ローカル開発用Docker Compose

Railsの規約と標準ライブラリを優先してください。リスクまたは複雑さを実質的に減らせる場合を除き、gemやJavaScriptパッケージを追加しないでください。追加する場合は、先に理由を説明してください。

## アーキテクチャのルール

- コントローラーは薄く保ってください。
- 採点、CSVインポート、教材インポートのロジックは、責務を限定したサービスオブジェクトに配置してください。
- 学習者が所有するレコードは、必ず現在のユーザーを起点に絞り込んでください。学習履歴や進捗を、スコープされていないIDだけで取得してはいけません。
- すべての `/admin` 操作を、サーバー側のロール認可で保護してください。
- UIの翻訳は `config/locales` に、学習教材はバージョン管理された教材ファイルまたはデータベース項目に保存してください。
- 日時はUTCで保存し、画面ではAsia/Tokyoとして表示してください。
- 通常のアプリケーション操作では、利用者や学習履歴を物理削除しないでください。
- マイグレーションはロールバック可能にするか、文書化したロールバック手順を用意してください。
- 学習履歴を消失させる可能性があるカスケード削除は避けてください。
- 信頼できないHTMLをそのまま表示しないでください。意図的にマークアップを許可する場合は、許可リスト方式でサニタイズしてください。

## 教材と多言語対応のルール

- 学習用の日本語本文は常に表示してください。
- 漢字を含む文章には、必要に応じてかな読みを表示してください。
- 学習者向けのUIテキストは、すべてI18nを通して翻訳してください。
- 必須対応言語は設定で管理し、必須翻訳が不足している教材は公開できないようにしてください。
- 言語のフォールバック順は、選択言語、英語、日本語とします。
- すべてUTF-8を使用してください。
- 英語やベトナム語の長い文字列を、幅360pxの画面で確認してください。
- 音声の再生に失敗しても、テキストによる学習を続けられるようにしてください。

## セキュリティと個人情報保護のルール

- `.env`、認証情報、APIキー、本番パスワード、実在する学習者情報、旅券番号、在留カード番号、住所、私用電話番号をコミットしないでください。
- パスワード、仮パスワード、Cookie、セッションIDをログへ出力しないでください。
- Active Recordを通じたパラメーター化クエリを使用してください。
- CSRF保護を有効なままにしてください。
- 本番環境のCookieには、Secure、HttpOnly、適切なSameSite属性を設定してください。
- ログイン、認可、レコード所有権、CSVインポート、教材インポートを変更する場合は、必ずテストを追加または更新してください。
- 破壊的なコマンド、本番環境での操作、元に戻せないマイグレーションには、明示的な人間の承認が必要です。

## 想定コマンド

リポジトリに実在するコマンドを使用してください。この一覧と `README.md` を常に最新の状態に保ってください。

```bash
bin/setup
bin/dev
bin/rails db:prepare
bin/rails test
bin/rails test:system
bundle exec rubocop
bundle exec brakeman -q
bundle exec bundler-audit check --update
bin/rails content:validate
```

## 各タスクの作業手順

1. 対象となる要件IDと受入条件を明示してください。
2. 関連するコードとテストを確認してください。
3. 複数ファイルにまたがる作業やリスクの高い作業では、編集前に短い実装計画を提示してください。
4. 要件を満たすために必要な、最小かつ一貫した変更を行ってください。
5. 正常系、認可、所有権、重要な異常系について、テストを追加または更新してください。
6. 最も対象範囲の狭い関連テストから実行し、その後に必要な全体チェックを実行してください。
7. 差分を確認し、仕様外の変更、機密情報、未翻訳のUIテキスト、認可漏れがないか確認してください。
8. 動作または設計上の判断を変更した場合は、`README.md`、`SPEC.md`、`TASKS.md`、`docs/decisions.md` のうち必要な文書を更新してください。
9. 作業完了時に、次の内容を報告してください。
   - 変更したファイル
   - 実装した動作
   - 実行したコマンドと結果
   - マイグレーションまたは運用への影響
   - 残っている制限事項

## 完了の定義

次の条件をすべて満たした場合のみ、タスクを完了とします。

- `SPEC.md` に記載された対象の受入条件を満たしている。
- 関連する自動テストが成功している。
- Lintとセキュリティチェックが成功している。例外がある場合は、その理由が文書化されている。
- 認可とレコード所有権が確認されている。
- 学習者向けテキストが多言語対応されている。
- モバイル画面でのレイアウトが考慮されている。
- 機密情報、実在する個人情報、デバッグコード、無関係なリファクタリングが含まれていない。
- 人が確認しやすい、目的の絞られた差分になっている。

---
title: 外国人労働者向け日本語学習Webアプリ 要件定義・基本設計書
subtitle: 「しごと日本語 / Work Nihongo」
version: 0.2
status: ドラフト（プロトタイプレビュー反映）
created: 2026-09-01
updated: 2026-09-01
project_period: 1か月（20営業日想定）
---

# 0. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 外国人労働者向け日本語学習Webアプリ 要件定義・基本設計書 |
| プロダクト名 | しごと日本語 / Work Nihongo |
| バージョン | 0.2 |
| 状態 | ドラフト（プロトタイプレビュー反映） |
| 作成日 | 2026年9月1日 |
| 改訂日 | 2026年9月1日 |
| 開発期間 | 1か月、20営業日を想定 |
| 開発環境 | Visual Studio Code + OpenAI Codex |
| 推奨実装 | Ruby on Rails 8系、PostgreSQL、Hotwire、Tailwind CSS、Docker |
| 対象 | プロジェクト責任者、開発者、教材担当者、翻訳担当者、受入テスト担当者 |
| 文書の位置づけ | 1か月MVPのスコープ、受入条件、基本設計、プロトタイプ適合化、開発手順の基準文書 |

## 0.1 この仕様書で確定している前提

- スマートフォンからの利用を中心とする。
- 日本語初級者を主対象とし、職場と日本での日常生活で使う表現を扱う。
- 学習者向け画面と管理者向け画面を用意する。
- プロダクト名は、日本語表記を「しごと日本語」、英語表記を「Work Nihongo」とし、ブラウザタイトル、ログイン画面、ヘッダー、フッター、利用規約で統一する。
- 初期対応言語は、日本語、英語、ベトナム語を仮定する。実際の利用者構成により、ベトナム語をインドネシア語、中国語などへ置き換えられる設計とする。
- MVPでは生成AI会話、発音自動採点、ビデオ授業、SNS機能、課金機能を実装しない。
- 教材翻訳と音声確認は、開発作業と並行して教材担当者が行う。
- ログイン、利用規約、プライバシー通知など明示的に公開する画面を除き、学習画面と管理画面は認証を必須とする。
- 画面ID、テスト用ラベル、デバッグ情報は設計資料と開発環境だけで使用し、本番・公開デモの利用者画面には表示しない。

## 0.2 開発開始日に決める事項

1. 対象業種: 製造、物流、農業、介護、外食など。
2. 初期対応言語: `ja`、`en`、`vi`をそのまま採用するか。
3. 初期利用人数、部署・事業所の分け方。
4. 本番配置先: 社内サーバー、AWS、Azure、GCPなどの会社承認環境。
5. ログインIDの規則と、個人情報・学習履歴の保存期間。
6. 教材と翻訳を最終承認する担当者。
7. 公開デモを提供するか。提供する場合は、検索エンジン公開、利用可能期間、問い合わせ先を決める。
8. 会社名・ロゴ・「公式」表記を使用する権限と承認者を決める。

## 0.3 改訂履歴

| バージョン | 日付 | 主な変更 |
|---|---|---|
| 0.1 | 2026-09-01 | 初版。1か月MVPの要件、基本設計、開発計画を定義。 |
| 0.2 | 2026-09-01 | プロトタイプレビューを反映。教材の関連整合性、選択肢言語、復習再回答、進捗状態、未認証アクセス、名称・デモ表示、受入テストを明確化。既存P0の品質条件を具体化したもので、機能範囲の追加を目的としない。 |

# 1. 企画概要

## 1.1 背景

日本で働く外国人労働者が、勤務前後や休憩時間に、仕事で頻繁に使う日本語を短時間で学べるWebアプリを開発する。一般的な文法学習よりも、「指示を聞く」「分からないと伝える」「安全を確認する」「遅刻や体調不良を連絡する」といった実務場面を優先する。

本アプリは、会社の正式な安全教育、業務マニュアル、法定教育を置き換えるものではなく、それらを理解しやすくするための補助教材とする。

## 1.2 目的

- 学習者が、仕事で必要な基本表現をスマートフォンで反復学習できるようにする。
- 日本語の漢字表記、かな読み、母語訳、音声を同じ画面で確認できるようにする。
- 小テストと間違い復習により、理解したつもりで終わらない仕組みを作る。
- 管理者が、誰がどの教材を完了したかを確認できるようにする。
- 1か月で社内試用可能なMVPを完成させる。

## 1.3 成功条件

MVPは、次の状態になった時点で成功と判定する。

- 学習者が、説明を受けなくてもログインし、言語を選び、1つのレッスンと小テストを完了できる。
- 未ログイン状態では保護された学習画面・管理画面を表示できず、ログアウト後にブラウザの戻る操作をしても保護画面の内容が再表示されない。
- 学習結果が保存され、再ログイン後も進捗を確認できる。
- 間違えた問題が復習一覧に追加され、学習者が再回答し、正解した場合だけ解決済みになる。
- ホーム、復習、進捗、管理画面で使用する「完了」「合格」「復習件数」が同じ定義と集計処理に基づいている。
- 公開レッスンでは、タイトル、学習目標、フレーズ、会話例、小テストが同じ学習テーマに対応し、別レッスンの教材が誤って混在していない。
- 各小テストは選択肢の言語を `option_locale` で明示し、1問内の選択肢を同じ実効言語・同じ表記形式で提示する。正解だけが言語や見た目で判別できない。
- 管理者が利用者の追加・停止、教材の公開状態変更、進捗確認を行える。
- 先行検証用の3レッスン「分からない・確認」「危険・停止・避難」「遅刻・欠勤・体調」が標準構成を満たし、対象利用者テストを完了している。
- 最低8レッスンが、必要な翻訳と音声を含む状態で登録されている。リリース目標は12レッスンとする。
- 利用者画面の名称が「しごと日本語 / Work Nihongo」に統一され、画面IDやデバッグ情報が表示されていない。
- P0の受入テストがすべて合格し、重大な不具合が残っていない。

# 2. 対象利用者と利用環境

## 2.1 ペルソナ

### 学習者

- 日本に来て働いている外国人労働者。
- 日本語レベルの目安は初級、JLPT N5〜N4程度。
- 会社支給または私物のスマートフォンを使用する。
- 長時間の学習より、1回5〜10分の短時間学習を好む。
- 漢字のみの文章は理解しづらい可能性がある。

### 管理者・教育担当者

- 学習者のアカウントを登録し、利用停止を行う。
- 教材の公開・非公開を管理する。
- 学習者ごとの完了状況や点数を確認する。
- 必要に応じて進捗をCSVで出力する。

### 教材担当者・翻訳担当者

- 職場で必要な日本語表現を選定する。
- 日本語、かな読み、母語訳、補足説明を作成する。
- 音声ファイルの発音と内容を確認する。
- MVPでは、教材ファイルを開発者へ渡し、開発者がインポートする運用を基本とする。

## 2.2 想定端末・ブラウザ

- AndroidスマートフォンのChrome。
- iPhoneのSafari。
- 管理画面はPCのChrome、Edge、Safari。
- 画面幅360px以上を基準とする。
- インターネット接続がある環境を前提とし、完全オフライン利用はMVP対象外とする。

# 3. スコープと優先順位

## 3.1 P0: 1か月MVPで必ず実装する機能

1. 管理者発行のログインIDとパスワードによるログイン、および未認証アクセスの遮断。
2. 初回ログイン時の表示言語選択とパスワード変更。
3. 学習ホーム、レッスン一覧、レッスン詳細。
4. 日本語、かな読み、母語訳、補足、音声再生。
5. 0.75倍速と1.0倍速の音声再生。
6. `option_locale` に従う同一言語・同一表記形式の選択肢による小テスト、採点、結果表示、再挑戦。
7. 間違えた問題への再回答と、正解時の自動解決を含む復習機能。
8. 定義を統一した学習進捗、完了数、合格数、復習件数、最高点の表示。
9. 管理者による利用者登録、停止、仮パスワード再発行。
10. CSVによる利用者一括登録。
11. 管理者による教材プレビュー、公開、非公開。
12. 管理者による学習進捗確認。
13. レッスン内の関連、重複、言語、問題数を含む教材YAMLの検証・インポート機能。
14. レスポンシブ表示、基本的なアクセシビリティ対応、プロダクト名称の統一、開発用表示の除去。
15. 自動テスト、セキュリティ検査、Docker化、手順書。

## 3.2 P1: 時間が残った場合に実装する機能

- 進捗CSV出力。
- 部署・事業所・職種による学習者絞り込み。
- 学習者によるフレーズのお気に入り登録。
- 管理画面からの教材本文編集。
- ホーム画面への連続学習日数表示。
- 管理者向け簡易集計グラフ。
- ホーム画面へのインストールを促すPWA設定。
- 教材の類似度検査や言語自動判定など、必須キー検査を超える高度な教材品質支援。

## 3.3 P2: 今回は実装しない機能

- 生成AIとの自由会話。
- 発音録音、音声認識、発音採点。
- ビデオ教材、ライブ授業、チャット、掲示板。
- SNSログイン、会社SSO、外部LMS連携。
- プッシュ通知、メール通知。
- 完全オフライン学習。
- 課金、広告、一般公開ユーザー登録。
- JLPT全範囲の学習コンテンツ。
- ネイティブスマートフォンアプリ。

# 4. 業務フロー

## 4.1 学習者の基本フロー

1. 管理者からログインIDと仮パスワードを受け取る。
2. ログインする。
3. 初回のみ表示言語を選択し、パスワードを変更する。
4. ホーム画面で「次に学ぶレッスン」を選ぶ。
5. フレーズと会話例を読み、音声を聞く。
6. 小テストに回答する。
7. 点数と解説を確認する。
8. 間違えた問題を復習画面で再回答する。
9. 復習で正解した問題が解決済みになり、不正解の問題は未解決のまま残ることを確認する。
10. 進捗画面で完了数、合格数、最高点を確認する。
11. ログアウトする。

## 4.2 管理者の基本フロー

1. 管理者としてログインする。
2. 利用者を個別登録またはCSV登録する。
3. 教材インポート後の内容を、対象言語を切り替えながらプレビューする。
4. 自動検証結果と教材レビュー用チェックリストを確認する。
5. タイトル、目標、フレーズ、会話例、小テストの対応関係に問題がなければ教材を公開する。
6. 利用者ごとの進捗、最終学習日、最高点を確認する。
7. 退職者や対象外利用者を停止する。

## 4.3 教材更新フロー

1. 教材担当者が所定のYAML形式で教材を作成する。
2. 翻訳担当者が母語訳を入力する。
3. 音声ファイルを所定のファイル名で配置する。
4. 開発者が `content:validate` を実行する。
5. 必須項目、件数、参照先、言語キー、重複、音声ファイルを検査し、エラーと警告を確認する。
6. エラーがなければステージングへインポートする。
7. 教材担当者が、内容の意味、テーマとの一致、翻訳、`option_locale` と選択肢の公平性を画面で確認する。
8. 管理者が公開する。

# 5. 機能要件

## 5.1 学習者機能

| ID・優先度 | 要件 | 受入条件 |
|---|---|---|
| FR-L01 / P0 | ログイン | 有効なログインIDとパスワードでログインできる。不正な組み合わせでは、どちらが誤っているかを特定しない共通エラーを表示する。未ログインで保護ページへアクセスした場合はログイン画面へ遷移する。 |
| FR-L02 / P0 | 初回設定 | 初回ログイン時に表示言語を選び、仮パスワードを変更するまで学習画面へ進めない。 |
| FR-L03 / P0 | 表示言語変更 | 設定画面から表示言語を変更できる。変更後、メニュー、問題文、解説、教材の母語訳、および `option_locale: learner` の選択肢に反映される。`option_locale: ja` の選択肢は日本語のままとする。 |
| FR-L04 / P0 | ホーム | 次の未完了レッスン、前回の続き、完了レッスン数、未解決の復習問題数を表示する。復習件数は `resolved_at IS NULL` のユニークな問題数とし、復習一覧の件数と一致する。 |
| FR-L05 / P0 | レッスン一覧 | 公開済みレッスンを順番に表示し、学習状態を未着手・学習中・完了、結果状態を未受験・不合格・合格として区別する。最高点80点以上は必ず合格と表示する。全レッスンを自由に選択できる。 |
| FR-L06 / P0 | レッスン閲覧 | タイトル、学習目標、フレーズ、かな読み、母語訳、利用場面、会話例を表示する。表示するフレーズと会話例は、現在のレッスンに所属するデータだけとする。 |
| FR-L07 / P0 | 音声再生 | フレーズごとに再生、一時停止、先頭から再生ができる。速度は0.75倍と1.0倍を選択できる。 |
| FR-L08 / P0 | 音声欠落時の処理 | 音声が存在しない場合でも画面は利用でき、テキストで学習を継続できる。 |
| FR-L09 / P0 | 小テスト開始 | レッスン内の全フレーズを閲覧後、小テストを開始できる。開始対象の問題は現在のレッスンに所属する公開済み問題だけとする。 |
| FR-L10 / P0 | 小テスト回答 | 1問ずつ選択肢を選び、次の問題へ進める。二重送信で回答が重複しない。各問題は `option_locale` を `learner` または `ja` で指定する。`learner` は学習者の選択言語、`ja` は日本語を実効言語とし、1問内の全選択肢を同じ実効言語・同じ表記形式で表示する。正解だけが別言語、別文字種、強調表示になる状態を禁止する。 |
| FR-L11 / P0 | 採点 | 正答数から0〜100点を算出する。80点以上を合格とするが、不合格でも次のレッスンへ進める。最高点は完了済みattemptの最大値とする。 |
| FR-L12 / P0 | 結果表示 | 点数、正答数、各問題の正誤、正解、解説、再挑戦ボタンを表示する。結果画面の問題、正解、解説は受験した問題のスナップショットまたは同一教材版を参照する。 |
| FR-L13 / P0 | 復習 | 間違えた問題を未解決の復習一覧へ追加する。正解は回答前に表示しない。学習者が再回答して正解した場合だけ自動的に解決済みとし、不正解の場合は未解決のまま残す。回答なしで任意に解決済みにする操作は設けない。 |
| FR-L14 / P0 | 進捗 | 完了レッスン数、合格レッスン数、レッスン別最高点、最終学習日時を表示する。ホーム、レッスン一覧、進捗、管理画面で同一の集計サービスを使用する。 |
| FR-L15 / P0 | 再開 | レッスン途中で画面を閉じても、再ログイン後に同じレッスンから再開できる。小テスト途中の再開はP1とし、MVPでは再開始でもよい。 |
| FR-L16 / P0 | ログアウト | ログアウト後はセッションを無効化し、保護画面へ `Cache-Control: no-store` 相当を設定する。ブラウザの戻る操作をしても学習画面の内容を再利用できず、再アクセス時はログイン画面へ遷移する。 |
| FR-L17 / P0 | セッション切れ | セッション切れ時は、選択言語でメッセージを表示してログイン画面へ遷移する。 |
| FR-L18 / P0 | 未認証時のナビゲーション | ログイン画面では学習者用の「ホーム」「学ぶ」「復習」「進捗」「設定」ナビゲーションを表示しない。公開ページから保護ページへのリンクを表示する場合は、ログインが必要であることを明示する。 |
| FR-L19 / P0 | プロダクト表示 | ブラウザタイトル、ログイン画面、ヘッダー、フッター、利用規約で「しごと日本語 / Work Nihongo」を使用する。「S-10」などの画面IDやデバッグ情報を利用者画面へ表示しない。 |
| FR-L20 / P0 | 公開デモ表示 | 公開デモを提供する場合は、表示される利用者名・履歴が架空データであること、非公式プロトタイプである場合はその旨を全ページから確認できる場所に表示する。承認のない会社名、会社ロゴ、「公式」表記を使用しない。 |

## 5.2 管理者機能

| ID・優先度 | 要件 | 受入条件 |
|---|---|---|
| FR-A01 / P0 | 管理者認可 | 管理者ロールのみ `/admin` 配下へアクセスできる。学習者は403相当画面またはホームへ遷移する。未ログイン利用者はログイン画面へ遷移する。 |
| FR-A02 / P0 | 利用者一覧 | ログインID、表示名、言語、部署・職種、状態、最終ログイン、最終学習日を表示・検索できる。 |
| FR-A03 / P0 | 利用者登録 | 必須項目を入力して利用者を登録し、仮パスワードを発行できる。 |
| FR-A04 / P0 | CSV一括登録 | 指定形式のCSVを取り込み、成功件数と失敗行・理由を表示する。既存ログインIDを重複登録しない。 |
| FR-A05 / P0 | 利用停止 | 利用者を論理停止できる。学習履歴は保持し、停止後はログイン不可とする。 |
| FR-A06 / P0 | 仮パスワード再発行 | 管理者が仮パスワードを再発行でき、利用者は次回ログイン時に変更を求められる。 |
| FR-A07 / P0 | 教材一覧 | 教材の版、レッスン名、翻訳状態、音声状態、検証状態、公開状態を表示する。エラー件数と警告件数を確認できる。 |
| FR-A08 / P0 | 教材プレビュー | 公開前教材を学習者画面と同等の表示で確認できる。対応言語を切り替え、タイトル、目標、フレーズ、会話例、小テストの対応関係を確認できる。 |
| FR-A09 / P0 | 公開・非公開 | 自動検証が成功し、教材担当者のレビューが完了した教材だけを公開できる。非公開にしても既存学習履歴は保持する。 |
| FR-A10 / P0 | 進捗一覧 | 学習者ごとの完了数、合格数、平均最高点、未解決復習問題数、最終学習日を表示する。学習者画面と同一の状態定義を使用する。 |
| FR-A11 / P1 | 進捗CSV | 現在の絞り込み条件で進捗をCSV出力できる。UTF-8 BOM付きCSVを基本とする。 |
| FR-A12 / P1 | 教材画面編集 | 管理画面から教材、フレーズ、問題、翻訳を編集できる。MVPではYAMLインポートを優先する。 |

## 5.3 教材インポート機能

| ID・優先度 | 要件 | 受入条件 |
|---|---|---|
| FR-C01 / P0 | YAML読込 | `db/content` 配下の教材YAMLを読み込める。 |
| FR-C02 / P0 | スキーマ・件数検証 | 必須項目、許可言語キー、フレーズ数、会話発話数、問題数、選択肢数、正解数、重複slug、音声ファイル有無を検査する。公開教材は6.1の標準件数を満たす。 |
| FR-C03 / P0 | 所属・参照整合性 | Phrase、Dialogue、DialogueLine、Quiz、QuizOptionが宣言されたLessonに所属し、`source_phrase_id` または `source_dialogue_line_id` が同じレッスン内の実在データを参照することを検査する。別レッスン参照はエラーとする。 |
| FR-C04 / P0 | 選択肢整合性 | `option_locale` は `learner` または `ja` のみ許可する。`learner` の場合は全選択肢に公開対象言語の同じキー、`ja` の場合は全選択肢に日本語キーが存在することを検査する。表示時に1問単位で同じ実効locale・同じ表示方式から値を取得し、正解だけ翻訳キーが異なる状態をエラーとする。 |
| FR-C05 / P0 | 重複検知 | 同一レッスン内の重複IDをエラーとする。異なるレッスン間で問題文・正解・選択肢セットが完全一致する場合は公開阻止エラーとする。意図的な再利用は `reuse_allowed: true` と空でない `reuse_reason` がある場合だけ許可し、検証レポートへ警告として残す。 |
| FR-C06 / P0 | 安全な更新 | slugと教材版をキーとして追加・更新し、履歴データを壊さない。削除は自動実行せず、非公開扱いとする。公開済みattemptが参照する問題内容は再現可能にする。 |
| FR-C07 / P0 | 検証レポート | エラー・警告ごとに、ファイル名、レッスンslug、教材ID、項目名、理由、修正候補を表示する。 |
| FR-C08 / P0 | 公開制御 | 必須翻訳、標準件数、参照整合性、小テスト要件が揃うまで公開不可とする。音声欠落は警告扱いにするか、会社判断でエラー扱いに設定できる。自動検証では判断できない意味の正しさは教材担当者の承認を必須とする。 |

# 6. 教材仕様

## 6.1 1レッスンの標準構成

- 学習時間: 5〜10分。
- 学習目標: 1つ。
- フレーズ: 5〜8個。
- 会話例: 1つ、4〜8発話。
- 小テスト: 3〜5問。
- 各小テスト: 原則3〜4選択肢。最低2選択肢。
- 日本語表記、かな読み、母語訳、短い補足を持つ。
- 音声は1フレーズ1ファイルを基本とし、会話全体音声はP1とする。
- 長い文法説明を避け、実際に発話または聞き取りで使う表現を優先する。
- 下書き・開発途中では標準件数未満を許容するが、公開状態へ変更する教材は上記の件数と品質条件を満たす。

## 6.2 表示ルール

- 日本語本文は常に表示する。
- 漢字を含む文には、かな読みを別行で表示する。
- 母語訳は選択中の言語を表示する。
- 翻訳が欠落している場合は、下書き・プレビューでは英語、次に日本語へフォールバックし、管理者へ欠落警告を表示する。公開判定では必須言語の欠落をエラーとする。
- 「やさしい日本語」を基本にし、1文を短くする。
- 危険、安全、禁止に関する表現は、曖昧な意訳を避けて教材担当者が確認する。
- 色だけで正誤や危険を表現せず、文字とアイコンを併用する。
- 画面に表示するタイトル、目標、フレーズ、会話例、小テストは、現在のレッスンに所属する同一教材版から取得する。

## 6.3 初期カリキュラム案

| No. | レッスン | 主な表現例 |
|---|---|---|
| 1 | あいさつ | おはようございます、よろしくお願いします、お先に失礼します |
| 2 | 自己紹介 | 名前、出身、担当、今日から働きます |
| 3 | 時間と勤務 | 何時から、何時まで、休憩、交代、残業 |
| 4 | 基本の指示 | してください、持ってきてください、待ってください、止めてください |
| 5 | 分からない・確認 | 分かりません、もう一度お願いします、ゆっくりお願いします、これでいいですか |
| 6 | 報告・連絡・相談 | 終わりました、問題があります、確認してください、手伝ってください |
| 7 | 数字・個数・単位 | 1個、2本、3枚、センチ、キロ、温度、時刻 |
| 8 | 道具と場所 | 右、左、上、下、入口、出口、置き場、片付ける |
| 9 | 安全装備・標識 | ヘルメット、手袋、保護具、立入禁止、注意 |
| 10 | 危険・停止・避難 | 危ない、止まって、触らないで、逃げて、119番 |
| 11 | 遅刻・欠勤・体調 | 遅れます、休みます、熱があります、けがをしました |
| 12 | 病院・災害・緊急連絡 | 病院へ行きたい、地震、火事、避難場所、連絡先 |

会社固有の機械名、製品名、作業手順は、共通教材と分離した「業種別・事業所別教材」として追加する。

## 6.4 小テスト形式・選択肢作成ルール

MVPでは、次の形式だけを実装する。

1. 日本語を見て母語訳を選ぶ。
2. 母語訳または場面を見て日本語を選ぶ。
3. 音声を聞いて意味または正しい返答を選ぶ。
4. 会話の空欄に入る表現を選ぶ。

選択肢は、次のルールを満たす。

- 各問題は `option_locale` を持つ。`ja` は全選択肢を日本語、`learner` は全選択肢を学習者が選択中の言語で表示する。日本語を選ぶ問題は原則 `ja`、母語訳を選ぶ問題は原則 `learner` とし、音声・会話問題は学習目標に応じて明示する。
- 正解だけ英語、正解だけ日本語、正解だけ太字など、内容を理解しなくても判別できる手掛かりを作らない。
- 選択肢の句読点、敬体・常体、文字種、概ねの長さを可能な範囲でそろえる。
- 誤答選択肢は学習テーマに関連するもっともらしい表現とするが、安全行動について危険な誤解を残す内容は解説で明確に否定する。
- 問題は `source_phrase_id` または `source_dialogue_line_id` で根拠教材を参照し、その参照先は同じレッスン内に限定する。
- 意図的に共通問題を再利用する場合は `reuse_allowed: true` と空でない `reuse_reason` に再利用理由を明記する。

記述式、並べ替え、録音、自由回答はP2とする。

## 6.5 教材内容の整合性・公開条件

各レッスンは、次の観点で自動検証と人によるレビューを行う。

- タイトルと学習目標が一致している。
- フレーズと会話例が学習目標の達成に必要な内容である。
- 小テストがそのレッスンで学んだフレーズまたは会話を出題している。
- 日本語、かな、各言語の翻訳、解説、音声の意味が一致している。
- 別レッスンのフレーズ、会話、問題、解説が誤って混在していない。
- 正解が1つであり、`option_locale` に従って選択肢の実効言語・形式が統一されている。
- 同じ問題が複数レッスンへ意図せず複製されていない。
- 危険、停止、避難、体調、災害の表現は、教材責任者が必ず内容を承認する。

自動検証は参照、件数、キー、完全一致重複などを対象とする。翻訳の自然さや意味の一致は完全には自動判定できないため、教材担当者・翻訳担当者による目視確認を公開条件とする。

## 6.6 先行検証する3レッスン

全12レッスンを一度に薄く作成する前に、次の3レッスンを標準構成で完成させる。

1. 分からない・確認。
2. 危険・停止・避難。
3. 遅刻・欠勤・体調。

3レッスンについて、日本語、かな、英語、ベトナム語、音声、会話例、小テスト、誤答復習、進捗反映を完成させ、対象利用者3〜5人の試用結果を反映してから残り教材へ展開する。

## 6.7 復習教材の表示・集計ルール

- 復習一覧の1件は、未解決のユニークなQuiz 1問を表す。
- `wrong_count` はその問題を間違えた累計回数であり、復習件数には使用しない。
- 回答前は問題と選択肢を表示し、正解・解説は隠す。
- 回答後に正誤、正解、解説を表示する。
- 正解時は `resolved_at` を設定し、不正解時は未解決のまま `wrong_count` と最終回答日時を更新する。
- 解決済み問題を再度間違えた場合は、同じReviewItemを未解決へ戻す。

# 7. 画面設計

## 7.1 共通UI方針

- 学習者画面はスマートフォンファーストとする。
- 本文文字サイズは原則16px以上、主要ボタンは片手で押しやすい大きさとする。
- 認証後の学習者画面の下部ナビゲーションは「ホーム」「学ぶ」「復習」「進捗」「設定」の5項目とする。
- ログイン画面、利用規約、プライバシー通知には、認証後の学習者用ナビゲーションを表示しない。
- 1画面の主要操作は1つに絞り、「次へ」「回答する」「もう一度学ぶ」など明確な文言を使う。
- アイコンだけのボタンにはアクセシブルな名前を付ける。
- ローディング、エラー、成功メッセージを選択言語で表示する。
- ブラウザタイトル、ヘッダー、フッター、ログイン画面の名称を「しごと日本語 / Work Nihongo」に統一する。
- `S-10`、`FR-L01`などの設計ID、fixture名、デバッグ値、内部主キーを利用者画面へ表示しない。必要な場合は開発環境だけで有効にする。
- 「復習する問題」と「最近の間違い回数」など意味が異なる数値を、同じ「件数」ラベルで表示しない。
- 管理者画面はPC利用を中心とし、左サイドメニューまたは上部メニューを使用する。

## 7.2 画面一覧

| 画面ID | 画面名 | 主な要素 | 備考 |
|---|---|---|---|
| S-01 | ログイン | ログインID、パスワード、ログインボタン、言語切替 | 学習者ナビは非表示。パスワード再設定は管理者対応 |
| S-02 | 初回設定 | 表示言語、パスワード変更、利用規約・プライバシー表示 | 完了まで学習画面へ進めない |
| S-10 | ホーム | 次のレッスン、前回の続き、完了数、未解決復習問題数 | 最優先の学習導線 |
| S-20 | レッスン一覧 | コース、学習状態、結果状態、所要時間、最高点 | すべて選択可能 |
| S-21 | レッスン詳細 | 目標、フレーズカード、かな、訳、補足、音声、会話例 | 前へ・次へ、進捗バー |
| S-22 | 小テスト | 問題、`option_locale` に従う同一言語の選択肢、音声、回答ボタン、問題番号 | 1問1画面を基本 |
| S-23 | 結果 | 点数、合否、正誤、解説、再挑戦、次のレッスン | 80点以上を合格 |
| S-30 | 復習 | 未解決問題、選択肢、回答、正誤、解説 | 正解時のみ自動解決 |
| S-40 | 進捗 | 完了数、合格数、レッスン別最高点、最終学習日 | グラフはP1 |
| S-50 | 設定 | 表示言語、パスワード変更、ログアウト | 個人情報編集は最小限 |
| A-10 | 管理ダッシュボード | 利用者数、直近学習者、未学習者、教材状態 | 集計グラフはP1 |
| A-20 | 利用者一覧・編集 | 検索、登録、CSV、停止、仮パスワード | 操作結果を明示 |
| A-30 | 教材一覧 | 翻訳状態、音声状態、検証状態、版、公開、プレビュー | 本文編集はP1 |
| A-40 | 進捗一覧 | 利用者、部署、完了数、合格数、未解決復習数、平均最高点、最終学習日 | CSVはP1 |

画面IDは仕様書、テスト名、開発者向けログで使用する識別子であり、利用者向け画面には表示しない。

## 7.3 主要画面の表示仕様

### S-01 ログイン・未認証時

- ヘッダーまたはロゴとして「しごと日本語」、補助表記として「Work Nihongo」を表示する。
- 学習者用の下部ナビゲーションは表示しない。
- 未認証で保護URLを開いた場合は、ログインが必要であることを選択言語で表示する。
- 公開デモの場合は、架空データであることと、非公式プロトタイプであればその旨を表示する。

### S-10 ホーム

- ヘッダー: プロダクト名、利用者名。MVPでは通知を表示しない。
- メインカード: 「次に学ぶ」レッスンのタイトル、所要時間、開始・続きからボタン。
- サブカード: 完了レッスン数、合格レッスン数、未解決の復習問題数。
- 復習件数はS-30に表示される未解決問題数と一致させる。
- 間違いの累計回数を表示する場合は、「最近の間違い」など別の名称とする。
- すべて完了した場合: 「復習する」「最初から見直す」を表示する。

### S-20 レッスン一覧

- 学習状態は、未着手・学習中・完了を表示する。
- 結果状態は、未受験・不合格・合格を表示する。
- 最高点が80点以上の場合、結果状態は必ず合格とする。
- 表示領域が狭い場合でも、学習状態と結果状態を同じ語へ混在させない。

### S-21 レッスン詳細

- 画面上部: レッスン名、目標、現在位置。
- フレーズカード: 日本語、かな、母語訳、利用場面、音声再生、速度変更。
- 会話例: 発話者を区別し、各発話に日本語・かな・訳を表示する。
- 画面下部: 前へ、次へ。最後のカードでは「小テストへ」。
- 音声再生中に別音声を選んだ場合、前の音声を停止する。
- 表示内容はすべて現在のLessonと同じ教材版に所属するものとする。

### S-22 小テスト

- 問題番号と総問題数を表示する。
- 回答前は次へ進めない。
- 1問の選択肢は、`option_locale` から決定した同じ実効言語、同じ文字種、同じ視覚スタイルで表示する。
- 回答確定後の即時正誤表示は行わず、全問終了後にまとめて表示する。
- ブラウザ再送信や連打による重複回答を防ぐ。

### S-30 復習

- 回答前は、問題、必要な音声、選択肢、間違えた回数、最終誤答日時を表示する。
- 回答前に正解や解説を表示しない。
- 回答後に正誤、正解、解説を表示する。
- 正解した場合は自動的に解決済みとし、一覧から除外する。不正解の場合は未解決のまま残す。
- 「解決済みにする」だけを行うボタンは設けない。

### 共通ヘッダー・フッター

- プロダクト名の表記ゆれを禁止する。
- 会社名、会社ロゴ、制作会社表記は、使用承認を得た環境だけで表示する。
- 非公式プロトタイプでは「個人制作による非公式プロトタイプ」など、誤認を防ぐ文言を表示する。
- 画面ID、開発者名、内部URL、テストアカウントの資格情報を表示しない。

### A-20 利用者CSV登録

CSV列は次を基本とする。

```csv
login_id,display_name,locale,department,job_type
A0001,Nguyen Van A,vi,工場1,製造
A0002,Maria Santos,en,物流,仕分け
```

- `login_id` は組織内で一意。
- `locale` は許可済み言語コードのみ。
- 文字コードはUTF-8。Excel利用を考慮し、テンプレートはBOM付きで配布する。
- 取込前にプレビューし、確定操作後に登録する。

# 8. データ設計

## 8.1 エンティティ関係

- Organization 1件に、複数のUserが所属する。
- Course 1件に、複数のLessonが所属する。
- Lesson 1件に、複数のPhrase、Dialogue、Quizが所属する。
- Dialogue 1件に、複数のDialogueLineが所属する。
- Quiz 1件に、複数のQuizOptionが所属する。
- Quizは、同一Lesson内のPhraseまたはDialogueLineを出題根拠として参照できる。
- UserはLessonProgressとLessonAttemptを複数持つ。
- LessonProgressは、レッスン閲覧開始、最終閲覧位置、全フレーズ閲覧完了を利用者・レッスン単位で保持する。
- LessonAttemptはQuizAnswerを複数持つ。
- Userは間違えたQuizに対するReviewItemを持つ。
- ReviewItemは再回答履歴としてReviewAnswerを複数持つ。

## 8.2 テーブル案

| テーブル | 主な項目 | 用途・制約 |
|---|---|---|
| organizations | id, name, code, default_locale | MVPは1組織でも保持し、将来の複数事業所対応に備える。codeは一意。 |
| users | organization_id, login_id, password_digest, role, display_name, locale, department, job_type, active, password_change_required, failed_attempts, locked_until, last_login_at | `[organization_id, login_id]` を一意にする。削除ではなく停止を基本とする。roleはlearner/admin。 |
| courses | slug, title_i18n, sort_order, published | title_i18nはJSONB。slugは一意。 |
| lessons | course_id, slug, title_i18n, objective_i18n, estimated_minutes, sort_order, content_version, validation_status, reviewed_by_user_id, reviewed_at, published_at | slugは一意。公開前に教材検証と人によるレビューを通し、承認者と日時を記録する。 |
| phrases | lesson_id, content_key, japanese_text, kana_text, translation_i18n, note_i18n, audio_path, sort_order | `[lesson_id, content_key]` を一意にする。日本語とかなは必須。translation_i18nはJSONB。 |
| dialogues | lesson_id, content_key, title_i18n, sort_order | `[lesson_id, content_key]` を一意にする。1レッスンに原則1件。 |
| dialogue_lines | dialogue_id, content_key, speaker, japanese_text, kana_text, translation_i18n, audio_path, sort_order | `[dialogue_id, content_key]` を一意にする。4〜8発話を標準とする。 |
| quizzes | lesson_id, content_key, kind, option_locale, source_phrase_id, source_dialogue_line_id, question_ja, question_kana, question_i18n, explanation_i18n, audio_path, reuse_allowed, reuse_reason, sort_order | `[lesson_id, content_key]` を一意にする。kindはmeaning、reverse、listening、dialogue。`option_locale` はlearner/ja。根拠参照は同じlesson内に限定する。`reuse_allowed=true`では空でないreuse_reasonを必須とする。 |
| quiz_options | quiz_id, content_key, text_ja, text_i18n, correct, sort_order | `[quiz_id, content_key]` を一意にする。1問につき正解は1つ。最低2選択肢、原則3〜4選択肢。必要なlocaleキーは親Quizの `option_locale` に従い、全選択肢で一致させる。 |
| lesson_progresses | user_id, lesson_id, last_content_position, viewed_all_at, last_viewed_at | `[user_id, lesson_id]` を一意にする。レッスン途中の再開、小テスト開始条件、未着手・学習中判定に使用する。 |
| lesson_attempts | user_id, lesson_id, content_version, status, started_at, completed_at, score, correct_count, question_count | statusはstarted/completed。scoreは0〜100。履歴として複数回保持する。受験した教材版を保存する。 |
| quiz_answers | lesson_attempt_id, quiz_id, quiz_option_id, correct, answered_at, question_snapshot, option_snapshot, explanation_snapshot | `[lesson_attempt_id, quiz_id]` を一意にし、重複回答を防ぐ。教材更新後も結果を再現できるようスナップショットを保持する案を採用する。 |
| review_items | user_id, quiz_id, wrong_count, last_wrong_at, last_reviewed_at, resolved_at | `[user_id, quiz_id]` を一意にする。誤答時にupsertし、解決済みなら未解決へ戻す。 |
| review_answers | review_item_id, quiz_option_id, correct, answered_at, option_snapshot | 復習での再回答履歴を保持する。正解時だけ親ReviewItemを解決済みにする。 |

## 8.3 多言語データ形式

JSONBの例:

```json
{
  "ja": "分かりません。もう一度お願いします。",
  "en": "I don't understand. Please say it again.",
  "vi": "Tôi không hiểu. Xin hãy nói lại một lần nữa."
}
```

- UI文言はRails I18nの `config/locales/*.yml` に配置する。
- 教材本文は教材YAMLからDBへインポートする。
- 下書き・プレビューで選択言語が欠落している場合は `en`、次に `ja` の順でフォールバックする。
- 公開判定時には、会社が必須と定めた言語がすべて存在することを確認する。
- 小テスト選択肢は、`option_locale` から1問単位の実効localeを決定して取得する。選択肢ごとに異なるフォールバックが発生する教材を公開しない。

## 8.4 データ保持と削除

- 利用者は原則として物理削除せず、`active=false` で停止する。
- 学習履歴の保存期間は会社規程に合わせ、開発開始日に決定する。
- 保存期間満了時は、利用者識別情報を匿名化し、集計に必要な履歴だけ残す方式を選択できる設計とする。
- パスワード、仮パスワード、セッションIDをログへ出力しない。
- 公開済み教材を更新しても、過去の受験結果と表示内容を再現できるよう、教材版または回答時スナップショットを保持する。

## 8.5 状態・集計の共通定義

| 項目 | 定義 |
|---|---|
| 未着手 | LessonAttemptがなく、LessonProgressもない。 |
| 学習中 | LessonProgressがある、または未完了LessonAttemptがあり、完了済みLessonAttemptがない。 |
| 完了 | 1件以上のLessonAttemptが `completed` である。点数に関係なく学習状態として完了。 |
| 未受験 | 完了済みLessonAttemptがない。 |
| 不合格 | 完了済みLessonAttemptがあり、最高点が80点未満。 |
| 合格 | 完了済みLessonAttemptの最高点が80点以上。 |
| 最高点 | 対象利用者・対象レッスンの完了済みLessonAttemptの `score` 最大値。 |
| 完了レッスン数 | 完了済みLessonAttemptが1件以上ある公開対象レッスンのユニーク数。 |
| 合格レッスン数 | 最高点が80点以上の公開対象レッスンのユニーク数。 |
| 復習件数 | `resolved_at IS NULL` のReviewItemのユニーク数。`wrong_count` の合計ではない。 |
| 最終学習日時 | レッスン閲覧、通常小テスト回答、復習回答のうち最も新しい日時。 |

これらの値は、画面ごとに個別実装せず、共通の集計クエリまたはサービスから取得する。

# 9. URL・画面遷移案

## 9.1 学習者向け

| Method | Path | 用途 | 認証 |
|---|---|---|---|
| GET | `/login` | ログイン画面 | 不要 |
| POST | `/session` | ログイン処理 | 不要 |
| DELETE | `/session` | ログアウト | 必要 |
| GET | `/terms` | 利用規約 | 不要 |
| GET | `/privacy` | プライバシー通知 | 不要 |
| GET/PATCH | `/initial-setup` | 初回設定 | 必要 |
| GET | `/` | ホーム | 必要 |
| GET | `/lessons` | レッスン一覧 | 必要 |
| GET | `/lessons/:slug` | レッスン詳細 | 必要 |
| POST | `/lessons/:slug/attempts` | 小テスト開始 | 必要 |
| GET | `/attempts/:id/questions/:position` | 小テスト問題 | 必要・所有者確認 |
| POST | `/attempts/:id/answers` | 回答保存 | 必要・所有者確認 |
| GET | `/attempts/:id/result` | 結果 | 必要・所有者確認 |
| GET | `/review` | 未解決の復習一覧 | 必要 |
| GET | `/review/:quiz_id` | 復習問題 | 必要・本人の未解決項目確認 |
| POST | `/review/:quiz_id/answers` | 復習回答、正解時の自動解決 | 必要・本人の未解決項目確認 |
| GET | `/progress` | 進捗 | 必要 |
| GET/PATCH | `/settings` | 設定 | 必要 |

回答を伴わない `/review/:quiz_id/resolve` は設けない。

## 9.2 管理者向け

| Method | Path | 用途 |
|---|---|---|
| GET | `/admin` | ダッシュボード |
| GET/POST | `/admin/users` | 利用者一覧・登録 |
| GET/PATCH | `/admin/users/:id` | 利用者編集・停止 |
| POST | `/admin/users/import` | CSV登録 |
| POST | `/admin/users/:id/reset-password` | 仮パスワード再発行 |
| GET | `/admin/lessons` | 教材一覧・検証結果 |
| GET | `/admin/lessons/:id/preview` | 教材プレビュー |
| PATCH | `/admin/lessons/:id/review` | 教材レビュー完了 |
| PATCH | `/admin/lessons/:id/publish` | 公開 |
| PATCH | `/admin/lessons/:id/unpublish` | 非公開 |
| GET | `/admin/progress` | 進捗一覧 |
| GET | `/admin/progress.csv` | 進捗CSV、P1 |

MVPではサーバーサイドレンダリングを基本とし、一般公開するJSON APIは作らない。公開ページ以外には共通認証フィルターを適用し、個別コントローラーで保護を付け忘れない構造にする。

# 10. システム構成・技術仕様

## 10.1 推奨技術スタック

1か月で完成させるため、フロントエンドとバックエンドを分離しない構成を推奨する。

| 分類 | 採用案 | 理由 |
|---|---|---|
| Webフレームワーク | Ruby on Rails 8系の開発開始時点の安定版 | 1リポジトリで認証、DB、管理画面、画面表示、テストを実装できる。 |
| UI | Hotwire/Turbo/Stimulus + Tailwind CSS | SPAを避け、必要な対話部分だけJavaScriptを使う。 |
| DB | PostgreSQLの会社サポート版 | JSONB、多言語データ、将来拡張に対応しやすい。 |
| 認証 | Railsセッション + `has_secure_password` | 外部認証サービスなしで社内ID方式を実装する。 |
| 音声 | MP3静的ファイルまたは会社承認オブジェクトストレージ | 再生品質を端末依存にしない。 |
| テスト | Minitest、Rails System Test | Rails標準を優先し、追加構成を減らす。 |
| 品質 | RuboCop、Brakeman、bundler-audit | コード規約、Rails脆弱性、依存関係を検査する。 |
| 開発環境 | VS Code、Docker Compose、Git | 開発端末差を小さくする。 |
| CI | GitHub Actionsまたは会社標準CI | テスト、Lint、セキュリティ検査を自動実行する。 |
| 配置 | Dockerイメージ + PostgreSQL | 配置先を会社承認環境へ合わせやすくする。 |

会社標準がReact/TypeScriptなどに固定されている場合でも、機能要件、データ要件、画面要件、受入条件はそのまま使用し、技術構成だけ置き換える。

## 10.2 アプリケーション構造案

```text
work-nihongo/
├── AGENTS.md
├── SPEC.md
├── TASKS.md
├── README.md
├── app/
│   ├── controllers/
│   │   ├── admin/
│   │   └── learner/
│   ├── models/
│   ├── views/
│   │   ├── admin/
│   │   └── learner/
│   ├── javascript/controllers/
│   ├── services/
│   │   └── content_import/
│   └── validators/
├── config/
│   └── locales/
├── db/
│   ├── content/
│   ├── migrate/
│   └── seeds.rb
├── test/
│   ├── models/
│   ├── requests/
│   ├── system/
│   └── fixtures/
├── docs/
│   ├── decisions.md
│   ├── operations.md
│   └── content-guide.md
├── Dockerfile
├── compose.yml
└── .env.example
```

## 10.3 設計原則

- Railsの標準的なMVCを優先し、不要な独自フレームワークを作らない。
- コントローラーを肥大化させず、教材インポート、採点、復習回答、進捗集計、CSV処理などは専用クラスへ分離する。
- 学習者向け文言をコードへ直書きせず、I18n経由で表示する。
- プロダクト名もI18nまたは共通設定から取得し、画面ごとに別名を直書きしない。
- 認証は公開ページの許可リスト方式で設計し、学習者・管理者の保護ページへ共通フィルターを必ず適用する。
- 管理者認可は共通フィルターで必ず適用し、画面の非表示だけで保護しない。
- attempt、ReviewItem、進捗は必ず `current_user` の所有範囲から取得する。
- 完了数、合格数、最高点、復習件数は共通の集計サービスを利用し、画面ごとに別のSQLや条件を書かない。
- すべてのDB更新にバリデーション、トランザクション、エラー処理を用意する。
- 教材の親子関係と出題根拠をDB制約とアプリケーション検証の両方で確認する。
- レッスン閲覧位置と全フレーズ閲覧完了はLessonProgressへ保存し、画面セッションだけに依存しない。
- 音声や教材が欠落してもアプリ全体が停止しない。ただし公開条件を満たさない教材は公開しない。
- MVPに不要な外部サービス依存を追加しない。
- 秘密情報、実在利用者データ、会社機密をGitへ登録しない。

# 11. 非機能要件

## 11.1 性能

- 通常の学習画面は、一般的な4G環境で3秒以内に主要内容が表示されることを目標とする。
- 音声は画面表示後に遅延読み込みし、初期表示を妨げない。
- 初期利用規模は登録500人以下、同時利用100人以下を想定する。
- 一覧画面はページネーションを使用し、全件を一度に描画しない。
- ホーム、進捗、管理者進捗の集計は共通クエリを使用し、必要なインデックスを設定する。

## 11.2 可用性・復旧

- 本番DBは日次バックアップを行う。保持期間は会社標準に合わせる。
- DBマイグレーションはロールバック可能にする。
- リリース前にステージングでマイグレーションと教材インポートを実行する。
- 教材公開失敗時は、直前の公開版を継続利用できるようにする。
- 誤った教材版を公開した場合は対象レッスンを非公開または直前版へ戻し、過去の受験結果を保持する。

## 11.3 アクセシビリティ

- キーボードだけでも主要操作が可能であること。
- フォームにはラベルとエラーメッセージを対応付けること。
- 正誤、危険、完了状態を色だけで表現しないこと。
- 音声内容と同じテキストを必ず表示すること。
- 文字拡大時にボタンや文章が重ならないこと。
- 動画・自動再生・点滅表現はMVPでは使用しない。
- 学習状態と結果状態は、色だけでなく「学習中」「完了」「合格」などの文字を表示する。

## 11.4 国際化

- 文字コードはUTF-8。
- サーバー時刻はUTC保存、表示はAsia/Tokyoを基本とする。
- 翻訳文字列が日本語より長くても、ボタンや表が壊れないこと。
- 人名は姓名の順序を固定せず、単一の表示名として扱う。
- 小テストの問題文・解説は学習者localeを使用し、選択肢は `option_locale` から決定した1問内で同じ実効言語のデータを使用する。
- フォールバックで選択肢の一部だけ別言語になる教材は公開しない。

## 11.5 ログ・監視

- エラー、教材インポート、教材検証、ログイン失敗、管理者操作の結果を記録する。
- パスワード、Cookie、セッションID、教材に不要な個人情報は記録しない。
- ログにはリクエストIDを含め、障害調査可能にする。
- 本番ではエラー通知先を設定する。使用サービスは会社承認に従う。
- 教材検証エラーにはlesson slug、content key、教材版を含めるが、学習者の回答内容を不要に記録しない。

# 12. セキュリティ・プライバシー

## 12.1 認証・認可

- パスワードは平文保存せず、強固なハッシュで保存する。
- 仮パスワードは初回ログインまたは再発行後に変更を必須とする。
- ログイン失敗が5回続いた場合、15分間ロックすることを初期案とする。
- セッションCookieにSecure、HttpOnly、SameSiteを適切に設定する。
- ログイン、利用規約、プライバシー通知以外は、原則として共通認証フィルターで保護する。
- 管理機能はサーバー側のロール判定で保護する。
- 保護画面には `Cache-Control: no-store, private` 相当を設定し、ログアウト後の戻る操作で内容が再利用されないようにする。
- ログアウト時はセッションIDを更新または破棄し、CSRF保護された非GET操作で実行する。

## 12.2 Webアプリ対策

- TLSを必須とする。
- CSRF対策を有効にする。
- 出力をエスケープし、教材HTMLを許可する場合は許可タグを限定してサニタイズする。
- SQLはActive Recordのバインドを使用し、文字列連結で条件を組み立てない。
- ファイルアップロードを追加する場合は、拡張子、MIME、容量、保存先を検証する。
- 管理者操作には確認画面または確認ダイアログを使用する。
- attempt、review、progressのIDをURLで変更しても、他利用者のデータを閲覧・更新できないこと。

## 12.3 個人情報最小化

- パスポート番号、在留カード番号、住所、私用電話番号は収集しない。
- 学習に不要な属性を登録しない。
- 実在データを開発・テスト・公開デモ環境へコピーしない。
- CSV出力権限を管理者に限定し、不要になった出力ファイルを削除する運用を定める。
- 公開デモでは、氏名、部署、履歴、点数を含め、すべて架空データを使用する。

## 12.4 Codex利用時のルール

- `.env`、APIキー、本番パスワード、実在利用者データをCodexへの指示に含めない。
- 会社が承認したリポジトリと端末で使用する。
- Codexが作成した変更は、人が差分を確認し、自動テストとセキュリティ検査に合格してから取り込む。
- ライブラリ追加、DB破壊操作、認証・認可変更は、目的と影響を確認してから実行する。
- 教材データの一括置換後は、別レッスン混入、問題重複、翻訳キー欠落を必ず検査する。

## 12.5 公開デモ・ブランド表示

- 公開デモは本番利用環境と分離し、環境変数などで `demo_mode` を明示する。
- デモ画面には「表示データは架空です」を常時確認できる位置に表示する。
- 会社承認前は、会社公式サービスと誤認させる会社名、会社ロゴ、制作会社表記を使用しない。
- 非公式プロトタイプの場合は、その旨と問い合わせ先をフッターまたは案内画面に表示する。
- 一般公開の準備が完了するまでは、必要に応じて `noindex`、アクセス制限、Basic認証のいずれかを適用する。
- デモ用資格情報を表示する場合は、デモ専用アカウントに限定し、管理者権限を与えず、定期的に初期化する。

# 13. テスト計画

## 13.1 自動テスト

### モデル・サービス

- Userの一意性、停止、ロック、パスワード変更必須。
- Lesson、Phrase、Dialogue、DialogueLine、Quizの必須項目と多言語データ検証。
- Phrase、DialogueLine、Quizの親Lessonと、Quizの出題根拠が一致すること。
- 1問に正解が1つだけ存在すること。
- `option_locale` が許可値であり、その指定に必要なlocaleキーが全選択肢に揃い、同じ実効localeで表示できること。
- 同一ID、同一問題セット、意図しないレッスン間重複の検出。`reuse_allowed=true`の場合は空でない`reuse_reason`を必須とする。
- LessonProgressの一意性、最終閲覧位置、全フレーズ閲覧完了、小テスト開始条件。
- 採点計算、最高点、完了・合格判定。最高点80点以上が必ず合格になること。
- 完了数、合格数、復習件数を各画面が同じ集計サービスから取得すること。
- 誤答時のReviewItem作成・再オープン、復習不正解時の継続、復習正解時の解決。
- 教材YAMLの正常系、必須項目欠落、標準件数不足、重複slug、不正言語、不正正解数、別レッスン参照、選択肢キー不一致。

### リクエスト・認可

- 未ログイン利用者が保護ページへアクセスできない。
- ログイン、利用規約、プライバシー通知は未ログインで表示できる。
- ログイン画面に学習者用ナビゲーションが表示されない。
- 学習者が管理画面へアクセスできない。
- 停止利用者がログインできない。
- 他利用者のattempt、ReviewItem、進捗をURL変更で閲覧・更新できない。
- 回答なしでReviewItemを解決するエンドポイントが存在しない、または拒否される。
- ログアウト後に保護ページへ再アクセスするとログイン画面へ遷移し、保護画面にno-store相当のヘッダーがある。
- CSVに不正行があっても正常行とエラー行が正しく報告される。

### システムテスト

1. 仮パスワードでログインし、言語とパスワードを設定する。
2. レッスンを開き、音声を再生し、小テストを完了する。
3. 1問内の全選択肢が `option_locale` に従った同一の実効言語で表示される。
4. 間違い問題が復習一覧へ出る。
5. 復習で不正解となり、未解決のまま残る。
6. 再度復習して正解し、解決済みになる。
7. ホームと復習一覧の復習件数が一致する。
8. 最高点80点以上のレッスンが合格表示になる。
9. 進捗と最高点が保存される。
10. ログアウト後、戻る操作または保護URLへの直接アクセスで内容が表示されない。
11. 利用者画面の名称が統一され、画面IDが表示されない。
12. 管理者が利用者を登録・停止する。
13. 管理者が教材をプレビュー・レビュー完了・公開・非公開にする。

## 13.2 手動テスト

- Android ChromeとiPhone Safariで主要フローを確認する。
- 画面幅360pxで横スクロールやボタン切れがないことを確認する。
- 英語、ベトナム語の長い翻訳でレイアウトが崩れないことを確認する。
- 各公開レッスンについて、タイトル、目標、フレーズ、会話例、小テストが同じテーマであることを確認する。
- 小テストの正解だけが別言語・別文字種・不自然な長さになっていないことを確認する。
- 翻訳担当者が日本語と母語訳の意味一致を確認する。
- 危険・停止・避難、体調、災害の表現を教材責任者が確認する。
- 低速回線で、音声読み込み失敗時にも学習できることを確認する。
- キーボード操作、フォーカス表示、読み上げ用ラベルを確認する。
- ログアウト後の戻る操作を実機ブラウザで確認する。
- 公開デモでは、架空データ・非公式表示・会社名使用可否を確認する。
- 3〜5人の対象利用者に先行3レッスンを操作してもらい、理解できない文言と迷った箇所を記録する。

## 13.3 受入基準

- P0の機能要件がすべて合格している。
- 主要システムテストが自動化され、CIで成功する。
- 重大度CriticalまたはHighの未対応セキュリティ指摘がない。
- データ消失、他人の履歴閲覧、管理者権限逸脱、未認証での保護画面表示の不具合がない。
- 公開教材に別レッスン内容の混入、誤った正解・翻訳、意図しない問題重複がない。
- すべての小テストで、1問内の選択肢が `option_locale` に従った同じ実効言語・同じ表記形式になっている。
- 復習は回答を必須とし、正解時だけ解決済みになる。
- ホーム、復習、進捗、管理画面の件数・状態が共通定義と一致する。
- 利用者画面のプロダクト名が統一され、画面ID・デバッグ情報が表示されない。
- リリース対象教材が検証済みで、教材担当者の承認を得ている。
- 先行3レッスンが標準構成を満たし、対象利用者テストを完了している。
- Sev1、Sev2の不具合が0件。Sev3以下は回避策と対応予定を記録する。

# 14. 運用・リリース

## 14.1 環境

- `development`: 開発者PC、Docker Compose。画面IDなどの開発支援表示を許可できる。
- `test`: 自動テスト専用DB。
- `staging`: 受入テスト、教材確認、本番同等構成。
- `demo`: 任意。架空データだけを使用し、非公式・デモ表示、アクセス制御またはnoindexを設定する。
- `production`: 会社承認環境。開発用表示を無効化する。

## 14.2 CIで実行する項目

```bash
bin/rails db:test:prepare
bin/rails test
bin/rails test:system
bundle exec rubocop
bundle exec brakeman -q
bundle exec bundler-audit check --update
bin/rails content:validate
```

`content:validate` は、必須項目、標準件数、参照整合性、`option_locale` と選択肢の必須キー、重複、音声ファイルを確認する。実際のコマンドは採用Gemと会社CIに合わせ、`AGENTS.md` と `README.md` を同期して更新する。

## 14.3 リリース手順

1. mainブランチのCI成功を確認する。
2. 本番DBバックアップを確認する。
3. ステージングでマイグレーションと教材インポートを実行する。
4. `content:validate` のエラーが0件であることを確認する。
5. 教材担当者が対象言語を切り替えて、タイトル、目標、フレーズ、会話例、小テスト、正解、解説を確認し、レビュー完了にする。
6. スモークテストを行う。
7. 本番へDockerイメージを配置する。
8. 本番マイグレーションを実行する。
9. 教材をインポートし、管理画面から公開する。
10. ログイン、1レッスン、小テスト、誤答復習、進捗、ログアウト、管理画面を確認する。
11. プロダクト名、画面ID非表示、会社名・デモ表示を確認する。
12. リリース記録と既知問題を更新する。

## 14.4 障害時の基本対応

- アプリ障害: 直前のDockerイメージへ戻す。
- マイグレーション障害: 事前に確認したロールバック手順を実行する。
- 教材障害: 対象レッスンを非公開または直前教材版へ戻し、アプリ本体のロールバックは避ける。
- 誤った利用者CSV: 対象利用者を停止し、監査ログと登録結果を確認する。
- 誤ったデモ表示・会社表示: 直ちに表示を停止し、承認状況とキャッシュを確認する。

# 15. 1か月開発計画

## 15.1 20営業日の実装計画

| 日 | 開発作業 | 完了条件 |
|---|---|---|
| Day 1 | 対象業種、言語、利用人数、配置先、保存期間、公開デモ、会社名・ロゴ使用可否を決定。仕様凍結。 | TBD項目が決まり、P0/P1/P2と名称・デモ方針に責任者が合意。 |
| Day 2 | Git、Rails、Docker、PostgreSQL、CI、VS Code、Codex、SPEC/AGENTS/TASKSを準備。 | 新規環境でREADME手順により起動でき、CIが空テストで成功。 |
| Day 3 | DB設計、Dialogue系・LessonProgress・復習回答を含むマイグレーション、モデル、fixture/seed。 | モデルテストが成功し、教材親子関係、閲覧進捗、サンプル組織・管理者を作成可能。 |
| Day 4 | ログイン、ログアウト、キャッシュ制御、ロック、ロール認可。 | 未認証アクセス、戻る操作、学習者・管理者の権限テストが成功。 |
| Day 5 | 初回設定、I18n、共通レイアウト、下部ナビゲーション、名称統一。 | 3言語で主要メニューを表示でき、ログイン画面に学習者ナビと画面IDが出ない。 |
| Day 6 | ホーム、レッスン一覧、進捗状態、共通集計サービス。 | 完了・合格・復習件数が共通定義で表示される。 |
| Day 7 | レッスン詳細、フレーズカード、かな、訳、会話例、所属スコープ、LessonProgress。 | スマホで1レッスンを最後まで閲覧・再開でき、別レッスンデータが混在しない。 |
| Day 8 | 音声再生、速度変更、欠落時フォールバック。 | Android/iPhoneで音声を確認。 |
| Day 9 | attempt作成、小テスト、回答保存、`option_locale` による選択肢言語制御。 | 3〜5問を重複なく回答でき、1問内で言語が混在せず、正解だけ別言語にならない。 |
| Day 10 | 採点、結果、最高点、完了・合格状態。 | 最高点80点以上が必ず合格となり、再挑戦を含む基本フローが完成。 |
| Day 11 | 復習一覧、再回答、誤答継続、正解時自動解決。 | 回答なしで解決できず、誤答から解決までのシステムテストが成功。 |
| Day 12 | 管理者の利用者一覧、登録、停止、仮パスワード再発行。 | 管理者の利用者管理フローが完成。 |
| Day 13 | CSV利用者登録、検証、結果表示。 | 正常・異常混在CSVを安全に処理。 |
| Day 14 | 教材YAML検証・インポート、参照整合性、重複検知、プレビュー、レビュー完了、公開・非公開。 | 履歴を壊さず教材を更新でき、不整合教材を公開できない。 |
| Day 15 | 管理者進捗一覧、モバイルUI調整、先行3教材統合。 | P0機能が一通り接続され、3教材が標準構成で動作する。 |
| Day 16 | モデル・リクエスト・システムテスト追加。 | 主要経路と教材品質条件がCIで自動検証される。 |
| Day 17 | セキュリティ、アクセシビリティ、性能、ログ、デモ・ブランド表示確認。 | Critical/High指摘を解消し、非公式・架空データ表示が適切。 |
| Day 18 | 対象利用者3〜5人による先行3教材の受入テスト、修正。 | 操作不能、理解不能、教材不一致の主要問題を解消。 |
| Day 19 | ステージング、本番手順、バックアップ、運用手順、残り教材統合。 | 最低8教材でリリースリハーサル成功。 |
| Day 20 | 本番リリース、デモ、引き継ぎ、予備日。 | 受入基準を満たし、既知問題を記録。 |

## 15.2 教材作成の並行計画

| 期間 | 教材担当作業 |
|---|---|
| Day 1〜3 | カリキュラム、用語、表現、危険表現、教材ID・参照ルールを決定する。 |
| Day 4〜8 | 先行3レッスンの日本語本文、かな読み、英語・対象母語訳、会話例を標準件数で作成する。 |
| Day 6〜10 | 先行3レッスンの音声を作成し、日本語話者が発音と間を確認する。 |
| Day 8〜11 | 先行3レッスンの小テスト、同一言語の選択肢、解説を作成し、YAMLへ入力する。 |
| Day 12〜14 | 先行3レッスンをステージングでレビューし、対象テーマとの不一致、誤訳、重複を修正する。 |
| Day 9〜15 | 残り5〜9レッスンの本文、翻訳、音声、小テストを順次作成する。 |
| Day 15〜18 | 対象利用者テストを行い、先行3レッスンの知見を残り教材へ反映する。 |
| Day 19 | 最低8レッスンのリリース対象教材を最終承認する。 |

開発者が翻訳と音声作成も兼任する場合は、リリース教材を5〜8レッスンに減らし、教材の標準件数、内容整合性、認証、復習、テスト品質を優先する。

# 16. 役割分担

| 役割 | 主な責任 |
|---|---|
| プロジェクト責任者 | 対象業種、言語、P0範囲、保存方針、配置先、公開デモ、会社名・ロゴ使用方針を決定し、受入判断を行う。 |
| 開発者 | 設計、実装、自動テスト、CI、配置、運用手順、技術レビューを担当する。 |
| 教材責任者 | 日本語表現、かな、会話、問題、レッスン内の対応関係、危険表現を確認し、教材版を承認する。 |
| 翻訳担当者 | 母語訳を作成し、自然さと意味の一致を確認する。 |
| 日本語音声確認者 | 音声の発音、速度、アクセント、誤読を確認する。 |
| 対象利用者テスター | 実機で操作し、理解しづらい表現や操作を報告する。 |
| インフラ・セキュリティ担当 | 配置先、TLS、バックアップ、監視、公開デモのアクセス制御、社内AI利用規程を確認する。 |

# 17. Definition of Done

各タスクは、次を満たして完了とする。

- 仕様または受入条件と実装が一致している。
- 正常系と主要異常系の自動テストが追加されている。
- 関連する全テスト、Lint、セキュリティ検査、教材検証が成功している。
- 学習者向け文言とプロダクト名がI18nまたは共通設定から取得されている。
- 管理者権限、他利用者データ、未認証アクセス、ログアウト後の再表示を確認している。
- レッスンの親子関係、出題根拠、`option_locale`、選択肢の必須キー、正解数、重複を確認している。
- 完了、合格、最高点、復習件数が共通定義と一致している。
- 復習は回答を必須とし、正解時だけ解決済みになる。
- スマートフォン幅で表示確認している。
- 本番・公開デモの利用者画面に画面ID、デバッグ情報、未承認の会社表示が含まれていない。
- README、SPEC、TASKS、運用手順の変更が必要なら更新している。
- Codexが作成した差分を人が確認している。
- 不要なデバッグコード、秘密情報、実在個人データが含まれていない。

# 18. VS Code + Codexでの開発手順

## 18.1 リポジトリに置く文書

- `SPEC.md`: 要件と受入条件の唯一の基準。
- `AGENTS.md`: Codexが常に守るプロジェクトルール。
- `TASKS.md`: 20営業日のタスク、状態、完了確認。
- `README.md`: 起動、テスト、教材インポート、配置手順。
- `docs/decisions.md`: 重要な設計判断と理由。

CodexのVS Code拡張を使い、リポジトリ固有の継続的な指示は `AGENTS.md` に置く。大きな作業は最初に計画を作らせ、1回の依頼で1つの受入可能な変更に絞る。

## 18.2 Codexへの依頼テンプレート

```text
目標:
FR-L09「小テスト開始」を実装する。

参照:
SPEC.md の 5.1、8章、9.1。
AGENTS.md を必ず守る。

範囲:
- attempt作成
- 最初の問題表示
- 所有者チェック
- モデル・リクエスト・システムテスト

制約:
- MVPの範囲を広げない
- 新しいGemを追加する前に理由を示す
- 学習者向け文言はI18n化する
- 他利用者のattemptを閲覧できないこと

完了条件:
- 受入条件を満たす
- 関連テスト、RuboCop、Brakemanが成功する
- 変更ファイルと確認結果を最後に要約する
```

## 18.3 Codexを使う標準ループ

1. `SPEC.md`、`AGENTS.md`、対象タスクを読ませる。
2. 複数ファイルにまたがる作業は、実装前に計画を出させる。
3. 計画を確認し、対象機能だけ実装させる。
4. Codexにテスト、Lint、セキュリティ検査を実行させる。
5. 変更差分、DB変更、残課題、実行結果を説明させる。
6. 人が差分を確認し、必要な修正を依頼する。
7. 1機能1コミットを目安にGitへ記録する。
8. 同じ間違いが繰り返された場合、`AGENTS.md` にルールを追加する。

## 18.4 最初に使うCodexプロンプト

### プロンプト1: プロジェクト初期化

```text
SPEC.md、AGENTS.md、TASKS.mdを読んでください。
Day 2の範囲だけを実装してください。
Ruby on Rails 8系、PostgreSQL、Hotwire、Tailwind CSS、Minitest、Docker Composeを使用します。
まず実装計画と作成予定ファイルを示し、その後に実装してください。
READMEに、初回セットアップ、起動、テスト方法を書いてください。
空のテストでもCIが成功する状態まで確認してください。
```

### プロンプト2: データモデル

```text
SPEC.md 8章のデータ設計を実装してください。
Phrase、Dialogue、DialogueLine、Quizの同一Lesson所属、Quizの出題根拠、
LessonProgress、ReviewItemとReviewAnswer、教材版または回答スナップショットを含めてください。
マイグレーション、モデル、関連、DB制約、バリデーション、fixture、モデルテストを作成してください。
履歴を壊す自動cascade deleteは避けてください。
JSONBの多言語値を取得する共通メソッドを作り、localeのフォールバックと、
`option_locale` に応じた実効localeの決定、1問内の選択肢で異なるフォールバックが起きないことをテストしてください。
```

### プロンプト3: 認証と認可

```text
FR-L01、FR-L02、FR-L16、FR-L17、FR-L18、FR-A01を実装してください。
has_secure_passwordとRailsセッションを使用し、学習者と管理者を分離してください。
公開ページの許可リスト方式、保護画面のno-store相当、ログイン画面のナビ非表示を実装してください。
停止利用者、初回パスワード変更、セッション切れ、ログアウト後のアクセス、
ブラウザの戻る操作、管理者URLへの不正アクセスを自動テストしてください。
```

### プロンプト4: コードレビュー

```text
現在のgit diffを、SPEC.mdとAGENTS.mdに照らしてレビューしてください。
特に、認証・認可漏れ、ログアウト後の再表示、他利用者データ閲覧、二重送信、
教材の別レッスン参照、問題重複、`option_locale`・選択肢キー不一致、復習の無回答解決、
完了・合格・復習件数の定義ずれ、名称表記ゆれ、画面ID表示、I18n漏れ、
N+1、テスト不足、秘密情報を確認してください。
問題を重大度順に示し、P0/P1/P2の範囲を広げる提案は分けて記載してください。
```

### プロンプト5: 現行プロトタイプの適合化

```text
SPEC.md 21章のGAP項目を上から順に確認してください。
まず現状調査だけを行い、各GAPについて「適合・不適合・確認不能」、根拠ファイル、
影響範囲、必要なテストを表で示してください。
その後、GAP-01だけを修正してください。範囲外のUI変更や新機能追加は行わないでください。
修正後に関連テスト、content:validate、RuboCop、Brakemanを実行し、結果を要約してください。
```

# 19. リスクと対策

| リスク | 影響 | 対策 |
|---|---|---|
| 教材・翻訳・音声が間に合わない | システムが完成しても公開教材が不足する。 | 先行3レッスンを最初に完成させ、Day 1に担当者と締切を決める。必要なら5〜8レッスンへ縮小する。 |
| 別レッスンの教材が混入する | タイトル、本文、小テストが一致せず、誤学習につながる。 | 同一Lesson参照をデータ構造・検証・テストで強制し、公開前に目視レビューする。 |
| 問題の使い回し・誤った正解が残る | 教材への信頼を失い、進捗データも意味を失う。 | 問題セットの完全一致重複を検出し、正解・解説を翻訳担当者と教材責任者が確認する。 |
| 正解だけ別言語または別形式になる | 内容を理解せず正解でき、小テストが評価として機能しない。 | `option_locale` で1問単位の実効言語を指定し、全選択肢の必須キーと表示元を統一して自動・手動テストを行う。 |
| 復習を回答なしで解決できる | 学習効果を確認できず、復習件数が不正確になる。 | 復習回答APIだけで状態を更新し、正解時のみ解決する。直接解決APIを作らない。 |
| 完了・合格・復習件数の定義が画面ごとに異なる | 利用者と管理者が進捗を信用できない。 | 共通集計サービスと8.5の定義を使用し、画面間一致テストを追加する。 |
| 生成AI会話などの追加要求 | 1か月で品質が確保できない。 | P2として次期開発へ回し、今回の受入条件に含めない。 |
| 対象言語が途中で変わる | 翻訳と画面確認がやり直しになる。 | Day 1に初期言語を凍結し、追加言語はデータ追加で対応できる設計にする。 |
| 実機で音声が再生できない | 学習の主要価値が下がる。 | Day 8までにAndroid/iPhoneで確認し、MP3形式と配信ヘッダーを固定する。 |
| 権限漏れ | 個人の学習履歴が他人に見える。 | 公開ページ許可リスト、所有者スコープ、管理者認可、リクエストテストをP0にする。 |
| ログアウト後に画面が戻る操作で見える | 共用端末で学習履歴が漏れる。 | セッション破棄、no-store、実機での戻る操作テストを行う。 |
| 公開デモが会社公式サービスと誤認される | 会社名・信用・契約上の問題につながる。 | 架空データと非公式表示を明示し、会社名・ロゴは承認後だけ使用する。 |
| Codexが範囲外変更を行う | 差分が大きく、レビュー困難になる。 | 1依頼1機能、計画先行、AGENTS.md、差分レビュー、テスト必須を徹底する。 |
| 多言語でUIが崩れる | ボタンや表が読めない。 | 長い翻訳テストデータを用意し、360px実機で確認する。 |
| 本番環境決定が遅れる | リリース作業が最終週に集中する。 | Day 1に配置先を決め、Day 2からDockerとCIを動かす。 |
| 開発者が教材作成も兼務する | 実装・テスト時間が不足する。 | 教材数を減らし、教材編集画面などP1を切る。 |

# 20. 未決事項・変更管理

## 20.1 未決事項

- 対象業種と、会社固有レッスンの内容。
- 英語以外の初期母語。
- 学習履歴の保存期間。
- 利用規約・プライバシー通知の会社文面。
- 本番URL、インフラ、監視、バックアップ保持期間。
- 利用者CSVに部署・職種を含めるか。
- 音声を録音するか、会社承認のTTSで事前生成するか。
- 公開デモを継続するか、アクセス制限またはnoindexを適用するか。
- 会社名・ロゴ・制作会社表記を使用できるか、および承認記録の保管方法。
- 公開済み問題の再現方法として、教材版固定と回答スナップショットのどちらを正式採用するか。

## 20.2 変更ルール

- P0追加は、同等工数のP0を削除する場合だけ認める。ただし、既存P0の認証・教材品質・データ整合性に必要な受入条件の具体化は範囲追加とみなさない。
- 仕様変更は `SPEC.md` の該当箇所と `TASKS.md` を同時更新する。
- DB設計、認証、個人情報、配置構成の変更は `docs/decisions.md` に理由を残す。
- 受入条件を変更した場合は、対応するテストも更新する。
- 教材の表示内容を変更した場合は、該当レッスンの教材版を更新し、必要に応じて翻訳・音声・小テストを一緒に再承認する。

# 21. 現行プロトタイプ適合化

本章は、現行プロトタイプを本仕様へ適合させるための優先修正項目である。すべてP0として上から順に対応し、各項目の自動テストまたは手動確認記録を残す。

| ID | 修正対象 | 必須対応 | 完了条件 |
|---|---|---|---|
| GAP-01 | 教材の対応関係 | 各レッスンのタイトル、目標、フレーズ、会話例、小テストを同じLesson・教材版へ結び付ける。共通サンプルの意図しない使い回しを除去する。 | 12レッスンを走査し、別レッスン参照と未許可の完全一致問題が0件。先行3レッスンは標準構成を満たす。 |
| GAP-02 | 復習内容と操作 | 誤った正解・翻訳を修正する。回答前の正解表示と、回答なしの「解決済みにする」を廃止する。 | 不正解では未解決、正解では自動解決となるシステムテストが成功。 |
| GAP-03 | 小テスト選択肢 | 各問題に `option_locale` を設定し、1問内の選択肢をすべて同じ実効言語・同一形式で表示する。 | 英語・ベトナム語で、言語混在または正解だけ別言語になる問題が0件。 |
| GAP-04 | 認証・ログアウト | ログイン以外の学習ページを保護し、ログイン画面の学習者ナビを非表示にする。ログアウト後の戻る表示を防ぐ。 | 未認証の主要URLがログインへ遷移し、戻る操作でも保護内容が表示されない。 |
| GAP-05 | 名称・開発表示 | 「しごと日本語 / Work Nihongo」に統一し、画面IDを利用者画面から除去する。 | ブラウザタイトル、ログイン、ヘッダー、フッター、規約に表記ゆれがなく、`S-`形式の画面IDが表示されない。 |
| GAP-06 | 件数・状態 | 復習件数を未解決ユニーク問題数へ統一し、完了と合格を8.5の定義で表示する。 | ホームと復習一覧の件数が一致し、最高点80点以上はすべて合格表示。 |
| GAP-07 | 公開デモ表記 | 架空データ、非公式プロトタイプ、会社名・ロゴ使用可否を表示方針へ反映する。 | 実在データがなく、会社公式と誤認する表記がなく、デモであることを全ページから確認できる。 |

## 21.1 修正順序

1. GAP-01 教材の対応関係。
2. GAP-02 復習内容と操作。
3. GAP-03 小テスト選択肢。
4. GAP-04 認証・ログアウト。
5. GAP-05 名称・開発表示。
6. GAP-06 件数・状態。
7. GAP-07 公開デモ表記。

各GAPは原則として1機能1コミットで修正し、次のGAPへ進む前に関連テストを成功させる。

# 付録A. 教材YAML例

```yaml
course:
  slug: common-workplace
  title:
    ja: 仕事の基本
    en: Workplace Basics
    vi: Tiếng Nhật cơ bản tại nơi làm việc

lessons:
  - slug: ask-again
    content_version: 1
    sort_order: 5
    estimated_minutes: 7
    title:
      ja: 分からない・確認
      en: When You Do Not Understand
      vi: Khi bạn không hiểu
    objective:
      ja: 分からないときに、聞き返して確認できる。
      en: Ask someone to repeat an instruction and confirm it.
      vi: Có thể nhờ người khác nói lại hướng dẫn và xác nhận.
    phrases:
      - id: p01
        sort_order: 1
        japanese: 分かりません。
        kana: わかりません。
        translation:
          en: I do not understand.
          vi: Tôi không hiểu.
        note:
          ja: 分からないとき、すぐに言います。
          en: Say this immediately when you do not understand.
          vi: Hãy nói ngay khi bạn không hiểu.
        audio: ask-again/p01.mp3
      - id: p02
        sort_order: 2
        japanese: もう一度お願いします。
        kana: もういちど おねがいします。
        translation:
          en: Please say it again.
          vi: Xin hãy nói lại một lần nữa.
        note:
          ja: 聞き取れなかったときに使います。
          en: Use this when you could not hear clearly.
          vi: Dùng khi bạn không nghe rõ.
        audio: ask-again/p02.mp3
    dialogues:
      - id: d01
        title:
          ja: 指示を聞き返す
          en: Asking for an instruction again
          vi: Yêu cầu nhắc lại hướng dẫn
        lines:
          - id: dl01
            speaker: supervisor
            japanese: この箱を入口へ持ってきてください。
            kana: この はこを いりぐちへ もってきて ください。
            translation:
              en: Please bring this box to the entrance.
              vi: Hãy mang thùng này đến lối vào.
          - id: dl02
            speaker: learner
            japanese: すみません。もう一度お願いします。
            kana: すみません。もういちど おねがいします。
            translation:
              en: Excuse me. Please say it again.
              vi: Xin lỗi. Xin hãy nói lại một lần nữa.
    quizzes:
      - id: q01
        kind: meaning
        option_locale: learner
        source_phrase_id: p02
        reuse_allowed: false
        # trueにする場合は、reuse_reasonへ再利用理由を記載する。
        question_ja: 「もう一度お願いします。」の意味はどれですか。
        question_kana: 「もういちど おねがいします。」の いみは どれですか。
        question:
          en: What does this sentence mean?
          vi: Câu này có nghĩa là gì?
        explanation:
          ja: 聞き取れないときに、相手へ繰り返しをお願いします。
          en: Use it to ask the other person to repeat.
          vi: Dùng để nhờ người khác nói lại.
        options:
          - id: o01
            correct: true
            text:
              ja: もう一回言ってください。
              en: Please say it again.
              vi: Xin hãy nói lại.
          - id: o02
            correct: false
            text:
              ja: 少し待ってください。
              en: Please wait a moment.
              vi: Xin hãy đợi một chút.
          - id: o03
            correct: false
            text:
              ja: ここへ置いてください。
              en: Please put it here.
              vi: Xin hãy đặt nó ở đây.
          - id: o04
            correct: false
            text:
              ja: もう終わりました。
              en: I have already finished.
              vi: Tôi đã làm xong rồi.
```

実際の公開教材では、1レッスンにつきフレーズ5〜8個、会話4〜8発話、小テスト3〜5問を登録する。この付録はデータ構造の説明を目的とした抜粋例である。

# 付録B. 参考資料

- OpenAI Codex IDE extension: https://developers.openai.com/codex/ide
- OpenAI Codex custom instructions with AGENTS.md: https://developers.openai.com/codex/agent-configuration/agents-md
- OpenAI Codex best practices: https://developers.openai.com/codex/learn/best-practices

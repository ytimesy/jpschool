AGENTS.md

1. このファイルの目的

このリポジトリは、日本で働く外国人が、職場で必要な日本語を短時間で学ぶWebアプリ 「しごと日本語 / Work Nihongo」 を開発・改善するためのものです。

Codexは、実装、修正、レビュー、テスト、文書更新を行う際に、本ファイルを常設ルールとして扱ってください。

要件の正本は SPEC.md です。作業の進め方と貼り付け用プロンプトは CODEX_INSTRUCTIONS.md にあります。

2. 文書の優先順位

内容が競合する場合は、次の順で判断してください。

利用者が現在の会話または作業依頼で明示した指示

SPEC.md の要件、受入条件、非機能要件

本ファイル AGENTS.md の開発ルール

CODEX_INSTRUCTIONS.md の作業順・依頼テンプレート

README.md、docs/、既存コード内のコメント

競合を自分で推測して解消しないでください。変更前に競合箇所、影響、推奨案を報告し、人の判断を求めてください。

3. 公的枠組みの扱い

本アプリは、次の公的資料を参照して独自教材を構成します。

文部科学省・文化庁「日本語教育の参照枠」

厚生労働省「就労場面で必要な日本語能力の目標設定ツール」

厚生労働省「外国人就労・定着支援研修 標準モデルカリキュラム」

文部科学省「生活Can do」

次の表現を使用してください。

「公的枠組みを参照した」

「就労Can doに対応付けた」

「標準モデルカリキュラムを参考に再構成した」

次の表現は、正式な認定や審査を受けていない限り使用しないでください。

「国認定教材」

「厚生労働省認定」

「完全準拠」

「このコース修了だけでA1認定」

公的資料の文章を大量に転載しないでください。教材本文とCan doは、自社の対象職場に合わせて平易な表現へ再構成し、出典と対応関係をデータとして記録してください。

4. MVPの基本方針

1か月の改修では、機能数よりも次の品質を優先します。

A1相当の職場場面を、Can do中心に一貫して学べること

レッスン、会話例、小テスト、音声、進捗が同じ教材データへ正しく関連付くこと

「覚えた」ではなく「場面で行動できる」を学習目標にする課題達成型の設計にすること

選択式問題だけで話す力や書く力を認定しないこと

教材公開前に、日本語教育、職場実務、翻訳、安全の確認状態を管理できること

スマートフォンで迷わず操作できること

SPEC.md が明示的に変更されない限り、次はMVPへ追加しないでください。

生成AIとの自由会話

音声認識、発音自動採点

動画授業、ライブ授業、SNS、チャット

課金、広告、一般公開の利用者登録

ネイティブスマートフォンアプリ

完全オフライン学習

フロントエンドSPAとAPIの全面分離

大規模なゲーミフィケーション

5. 既存アプリの改修ルール

このプロジェクトは新規作成とは限りません。既存コードがある場合は、必ず先に現状を調査してください。

Gemfile、package.json、config/routes.rb、DBスキーマ、主要モデル、主要テストを確認する。

実際の技術構成を優先し、仕様書の推奨バージョンへ無断でアップグレードしない。

既存のURL、ログイン、学習履歴、教材slugを可能な限り維持する。

全面改修より、後方互換性を保った段階的な変更を優先する。

DB変更前に、既存データへの影響、バックフィル方法、ロールバック方法を示す。

公開中教材を自動削除しない。原則として非公開化または版管理を使う。

本番操作、データ削除、破壊的マイグレーションは、人の明示的承認なしに実行しない。

6. Codexの標準作業手順

各タスクでは、次の順序を守ってください。

対象となる SPEC.md の要件IDと受入条件を特定する。

関連するコード、データ、画面、テストを確認する。

編集前に、原因または現状、変更予定ファイル、実装計画、リスクを示す。

1回の作業を、レビュー可能な一つの目的へ絞る。

最小で一貫した変更を行う。

正常系、認可、所有権、重要な異常系、回帰不具合のテストを追加する。

対象を絞ったテストから実行し、その後に必要な全体検査を実行する。

差分を確認し、仕様外変更、機密情報、未翻訳UI、固定値、認可漏れを除く。

動作または設計判断を変えた場合は、関連文書も更新する。

完了報告を所定の形式で行う。

大きな変更を一度に実装しないでください。調査、設計、DB変更、画面変更、教材移行、テストを分割してください。

7. アーキテクチャとデータ整合性

コントローラーは薄く保つ。

採点、教材検証、教材インポート、CSV処理、Can do集計は責務を限定したサービスへ置く。

学習者が所有する履歴は、必ず現在の利用者を起点に取得する。

/admin 配下はサーバー側のロール認可で保護する。

画面で非表示にするだけの認可は認可として扱わない。

日時はUTCで保存し、表示は原則としてAsia/Tokyoにする。

通常操作では学習者と学習履歴を物理削除しない。

マイグレーションは原則ロールバック可能にする。

不要なgem、JavaScriptパッケージ、外部サービスを追加しない。追加前に理由、代替、保守影響を説明する。

教材関連では、次を必須の不変条件とします。

ホームのタイトル、説明、所要時間、リンクは、同一の next_lesson から取得する。

レッスンは主キー順ではなく、教材用の sort_order または position で並べる。

小テストは必ず表示中レッスンとの関連から取得する。

会話例、フレーズ、音声、解説も必ず表示中レッスンとの関連から取得する。

learner、admin、内部localeキーなどの内部値を利用者画面へ直接表示しない。

公開レッスンは少なくとも一つのCan doへ対応し、主Can doを一つ持つ。

同一コース内の並び順は重複させない。

二重送信で回答、自己評価、進捗が重複しないようにする。

8. 教材設計のルール

各レッスンは、原則として次の順で構成します。

今日のCan do

学習前の課題提示

モデル会話またはモデル文

意味、かな、母語訳、使う場面の確認

部分練習

課題遂行またはロールプレイ

理解確認

Can do自己評価

職場で試す課題または振り返り

教材本文は次を満たしてください。

日本語本文は常に表示する。

漢字を含む重要表現には、必要に応じてかな読みを付ける。

UI文言と教材翻訳を混同しない。UIはI18n、教材は教材データとして管理する。

必須言語は組織設定で管理する。

翻訳が不足している教材を公開しない。

フォールバック順は原則として選択言語、英語、日本語とする。

安全、医療、災害に関する重要情報は、学習者の理解可能な言語でも確認できるようにする。

「適当に」「なるべく早く」など、曖昧で現場事故につながる表現を教材の模範指示として使わない。

外国人に日本の職場慣行へ一方的に従わせる教材にしない。理由、相互確認、日本人側の伝え方も重視する。

AIが作った翻訳、音声、問題は、人が確認するまで承認済みにしない。

9. 評価のルール

評価は、次の種類を分けて扱ってください。

理解テスト: 聞く・読む内容を選択式等で自動採点する。

課題達成評価: 話す・やり取り・書く課題をルーブリックで評価する。

自己評価: 学習者が「一人でできる」「助けがあればできる」等を記録する。

観察評価: 上司、日本語講師、教育担当者が任意で記録する。

禁止事項:

選択式テストの点数だけで「会話ができる」と判定しない。

レッスン完了だけで「A1取得」「A1認定」と表示しない。

文法の細かな誤りだけを理由に、課題達成をすべて不合格にしない。

画面では、少なくとも次を区別してください。

学習完了

理解テスト合格

本人ができると自己評価

指導者が職場で確認

10. 多言語、アクセシビリティ、モバイル

文字コードはUTF-8とする。

学習者向けUI文字列をコードへ直書きしない。

画面幅360pxで主要画面を確認する。

長い英語・ベトナム語等でボタンやカードが崩れないようにする。

本文文字は原則16px以上とする。

正誤、危険、進捗を色だけで表現しない。

音声と同じテキストを表示する。

音声がない場合は、速度ボタンを非表示または無効にする。

キーボード操作、フォーカス表示、フォームラベル、エラー関連付けを確認する。

自動再生、点滅、不要なアニメーションは使用しない。

11. セキュリティと個人情報

.env、APIキー、認証情報、本番パスワードをコミットしない。

実在する利用者の氏名、在留カード番号、旅券番号、住所、私用電話番号をテストデータにしない。

パスワード、仮パスワード、Cookie、セッションIDをログへ出さない。

CSRF保護を有効に保つ。

Active Record等のパラメーター化されたクエリを使う。

Cookieには本番環境でSecure、HttpOnly、適切なSameSite属性を設定する。

認証、認可、所有権、CSV、教材インポートを変更したら必ずテストを追加する。

公開環境でデバッグ画面や内部例外を表示しない。

Codexへ本番データや会社機密を貼り付けない。

12. 必須の回帰テスト

既存不具合の再発防止として、少なくとも次を自動テストしてください。

ホームの「次に学ぶ」の表示内容と遷移先が同じレッスンである。

各レッスンでは、そのレッスン専用の小テストだけが表示される。

各レッスンでは、そのレッスン専用の会話例だけが表示される。

レッスン一覧と進捗一覧が公開レッスンを教材順に表示する。

内部キーが学習者画面へ露出しない。

音声未登録時も画面が利用でき、無効な再生操作を表示しない。

他人のattempt、自己評価、進捗をURL変更で閲覧できない。

学習者は管理画面へアクセスできない。

公開条件を満たさない教材を公開できない。

話す課題の達成状態が、選択式点数だけで自動確定しない。

13. コマンドの扱い

最初にリポジトリ内の実在するコマンドを確認してください。存在しないコマンドを作業報告へ記載しないでください。

Rails構成の場合の候補は次です。

bin/setup
bin/dev
bin/rails db:prepare
bin/rails test
bin/rails test:system
bundle exec rubocop
bundle exec brakeman -q
bundle exec bundler-audit check --update

教材検証コマンドを実装した場合は、README.md と本ファイルへ実際のコマンドを追記してください。

14. 明示的承認なしに行わないこと

本番デプロイ

本番DBへの接続または更新

データ削除

破壊的マイグレーション

Rails、Ruby、PostgreSQL等のメジャーアップグレード

認証方式の全面変更

課金、外部AI、外部音声サービスの導入

大規模な依存関係追加

公的資料の文章の大量転載

15. 完了報告の形式

各作業の最後に、次を簡潔かつ具体的に報告してください。

対象要件ID

調査結果または原因

変更したファイル

実装内容

DB・運用・後方互換性への影響

追加または更新したテスト

実行したコマンドと結果

未解決事項、既知の制限、次の推奨タスク

16. Definition of Done

次をすべて満たした場合のみ完了とします。

対象の受入条件を満たす。

関連する自動テストが成功する。

必要なLintとセキュリティ検査が成功するか、例外理由が記録されている。

認可と所有権を確認している。

学習者向けUIがI18n化されている。

360px幅を含むモバイル表示を確認している。

教材とCan doの対応が記録されている。

教材公開条件を迂回していない。

機密情報、実在個人情報、デバッグコード、無関係なリファクタリングを含まない。

人がレビューできる、目的の絞られた差分である。
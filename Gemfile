source 'https://rubygems.org'

ruby '3.3.9'

gem 'rails', '~> 8.0'
gem 'pg', '>= 1.0'
gem 'puma'
gem 'jbuilder'
gem 'sprockets-rails'

# Use Hotwire
gem 'turbo-rails'
gem 'stimulus-rails'

# Tailwind (Rails 8 では importmap/asset bundling 選択可)
gem 'tailwindcss-rails'

group :development, :test do
  gem 'sqlite3', '~> 1.4'
  gem 'byebug'
end

group :development do
  gem 'web-console'
  gem 'listen'
end
